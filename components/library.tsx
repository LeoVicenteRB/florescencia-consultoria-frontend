'use client';import {useState} from 'react';const docs=[['POPs','POP — Higienização das Mãos'],['POPs','POP — Processamento e Esterilização'],['manuais','Manual de Gestão da Qualidade'],['políticas','Plano de Segurança do Paciente'],['registros','Registro de Monitoramento da Esterilização'],['checklists','Checklist de Abertura da Clínica'],['planilhas','Controle de Treinamentos'],['guias','Guia de Implantação']];export function Library(){const [filter,setFilter]=useState('Todos');const fs=['Todos',...new Set(docs.map(d=>d[0]))];return <><div className="filters" role="group" aria-label="Filtrar documentos">{fs.map(f=><button className={`filter ${filter===f?'active':''}`} aria-pressed={filter===f} onClick={()=>setFilter(f)} key={f}>{f}</button>)}</div><div className="library">{docs.filter(d=>filter==='Todos'||d[0]===filter).map(([cat,title],i)=><article className="cover" key={title}><span className="eyebrow">{cat}</span><h3>{title}</h3><div><small>DOC-{String(i+1).padStart(3,'0')} · v1.0</small><br/><span className="stamp">PRÉVIA ILUSTRATIVA</span></div></article>)}</div></>}

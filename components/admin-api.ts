export const API_URL=process.env.NEXT_PUBLIC_API_URL??'http://localhost:3101/api';export function getAdminSession(){try{return JSON.parse(localStorage.getItem('florescencia.session')??'null')as{user:{name:string;email:string;role:string};accessToken:string}|null}catch{return null}}export async function adminFetch<T>(path:string,options:RequestInit={}){const session=getAdminSession();const response=await fetch(`${API_URL}/admin${path}`,{...options,headers:{...(!(options.body instanceof FormData)?{'Content-Type':'application/json'}:{}),Authorization:`Bearer ${session?.accessToken??''}`,...options.headers}});const data=await response.json();if(!response.ok)throw new Error(Array.isArray(data.message)?data.message.join(' '):data.message??'Não foi possível concluir a operação');return data as T}

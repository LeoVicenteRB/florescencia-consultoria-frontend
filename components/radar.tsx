'use client';import {useState} from 'react';const items=['POPs revisados','Controle de treinamentos','Registros de esterilização','Plano de segurança do paciente','Controle de limpeza','Documentos de radiologia','Gestão de resíduos','Registro de manutenção'];export function Radar(){const [selected,setSelected]=useState<string[]>([]);return <div className="radar"><p className="eyebrow" style={{color:'#dde9df'}}>EXEMPLO INTERATIVO</p><h2>Radar de preparo documental</h2><div className="checks">{items.map(i=><label className="check" key={i}><input type="checkbox" checked={selected.includes(i)} onChange={()=>setSelected(s=>s.includes(i)?s.filter(x=>x!==i):[...s,i])}/>{i}</label>)}</div><p><strong>{selected.length} de {items.length}</strong> áreas marcadas para visualização.</p><p className="micro" style={{color:'#dde9df'}}>Este exemplo ajuda a visualizar áreas que normalmente exigem organização documental. A avaliação real depende das características da clínica. As respostas não são armazenadas.</p></div>}

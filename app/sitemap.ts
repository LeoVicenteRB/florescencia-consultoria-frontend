import type{MetadataRoute}from'next';export default function sitemap():MetadataRoute.Sitemap{const base='http://localhost:3000';return['','/produto/sistema-gestao-inteligente','/documentos','/contato','/termos','/privacidade','/cookies'].map(path=>({url:base+path,lastModified:new Date()}))}

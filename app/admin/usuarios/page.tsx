'use client';import{useEffect,useState}from'react';import{adminFetch}from'@/components/admin-api';type User={id:string;name:string;email:string;role:string;status:string;createdAt:string};export default function Page(){const[data,setData]=useState<User[]>([]);useEffect(()=>{adminFetch<User[]>('/users').then(setData)},[]);return <><div className="admin-page-head"><div><p className="eyebrow">ACESSOS</p><h1>Usuários</h1><p>Consulte clientes, revisores e administradores cadastrados.</p></div></div><section className="admin-panel table-panel"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Usuário</th><th>Perfil</th><th>Status</th><th>Cadastro</th></tr></thead><tbody>{data.map(u=><tr key={u.id}><td><strong>{u.name}</strong><small>{u.email}</small></td><td>{u.role}</td><td><span className="admin-status active">{u.status}</span></td><td>{new Date(u.createdAt).toLocaleDateString('pt-BR')}</td></tr>)}</tbody></table></div></section></>}

'use client';import{useEffect,useState}from'react';import{adminFetch}from'@/components/admin-api';type Log={id:string;action:string;entity:string;entityId:string|null;createdAt:string;actor:{name:string;email:string}|null};export default function Page(){const[data,setData]=useState<Log[]>([]);useEffect(()=>{adminFetch<Log[]>('/audit-logs').then(setData)},[]);return <><div className="admin-page-head"><div><p className="eyebrow">RASTREABILIDADE</p><h1>Auditoria</h1><p>Histórico das ações administrativas relevantes.</p></div></div><section className="admin-panel table-panel"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Ação</th><th>Entidade</th><th>Responsável</th><th>Data</th></tr></thead><tbody>{data.map(l=><tr key={l.id}><td><strong>{l.action}</strong></td><td>{l.entity}<small>{l.entityId}</small></td><td>{l.actor?.name??'Sistema'}<small>{l.actor?.email}</small></td><td>{new Date(l.createdAt).toLocaleString('pt-BR')}</td></tr>)}{data.length===0&&<tr><td colSpan={4}><div className="admin-empty">Nenhuma ação administrativa registrada.</div></td></tr>}</tbody></table></div></section></>}

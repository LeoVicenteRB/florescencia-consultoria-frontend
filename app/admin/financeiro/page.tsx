'use client';import{useEffect,useState}from'react';import{adminFetch}from'@/components/admin-api';export default function Finance(){const[d,setD]=useState<any>();const[from,setFrom]=useState(''),[to,setTo]=useState('');const load=()=>adminFetch(`/platform/finance?${new URLSearchParams({...(from?{from}:{}),...(to?{to}:{})})}`).then(setD);useEffect(()=>{load()},[]);const money=(n=0)=>(n/100).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});return <><div className="admin-page-head"><div><p className="admin-kicker">FINANCEIRO</p><h1>Receita de assinaturas</h1><p>Indicadores calculados pela API, com gateway em modo mock.</p></div><div className="admin-period"><input type="date" value={from} onChange={e=>setFrom(e.target.value)}/><input type="date" value={to} onChange={e=>setTo(e.target.value)}/><button className="admin-button" onClick={load}>Filtrar</button></div></div>{d&&<><div className="admin-metrics"><article><small>MRR</small><strong>{money(d.mrrInCents)}</strong><span>{d.activeSubscriptions} assinaturas ativas</span></article><article><small>Taxas de inscrição</small><strong>{money(d.enrollmentRevenueInCents)}</strong><span>{d.newSubscriptions} novas no período</span></article><article><small>Recebido</small><strong>{money(d.receivedInCents)}</strong><span>{d.paidInstallments} parcelas</span></article><article><small>Inadimplência</small><strong>{money(d.overdueInCents)}</strong><span>{d.overdueInstallments} parcelas vencidas</span></article></div><div className="admin-card"><h2>Premissas do MVP</h2><p>Inscrição e mensalidade são calculadas exclusivamente no backend. O gateway MOCK deixa a primeira parcela paga e as demais pendentes para demonstrar o ciclo financeiro.</p></div></>}</>}
/* eslint-disable @typescript-eslint/no-explicit-any, react-hooks/exhaustive-deps */

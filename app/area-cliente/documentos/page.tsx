'use client';import{FormEvent,useEffect,useState}from'react';import{api}from'@/components/platform-api';const labels:any={PENDING_REVIEW:'Em análise',APPROVED:'Aprovado',REJECTED:'Rejeitado',CORRECTION_REQUIRED:'Correção solicitada',EXPIRED:'Vencido'};export default function Documents(){const[docs,setDocs]=useState<any[]>([]);const[msg,setMsg]=useState('');const load=()=>api<any[]>('/customer-documents').then(setDocs);useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);try{await api('/customer-documents',{method:'POST',body:fd});setMsg('Documento enviado para análise.');e.currentTarget.reset();load()}catch(x){setMsg(x instanceof Error?x.message:'Erro')}}return <><div className="portal-page-head"><div><p className="eyebrow">CONFORMIDADE LRSS</p><h1>Documentos da clínica</h1><p>Envie evidências próprias da clínica, acompanhe a revisão e as validades.</p></div></div><div className="portal-grid"><form className="portal-panel upload-form" onSubmit={submit}><h2>Novo documento</h2><label>Tipo<select name="type"><option value="LRSS">LRSS</option><option value="LICENCA_SANITARIA">Licença sanitária</option><option value="RESPONSABILIDADE_TECNICA">Responsabilidade técnica</option><option value="OUTRO">Outro</option></select></label><label>Título<input name="title" required/></label><label>Validade<input name="expiresAt" type="date"/></label><label>Arquivo PDF, PNG ou JPG<input name="file" type="file" accept=".pdf,.png,.jpg,.jpeg" required/></label><button className="btn primary">Enviar para revisão</button>{msg&&<p>{msg}</p>}</form><div className="portal-panel"><h2>Status dos envios</h2><div className="document-list">{docs.length?docs.map(d=><article key={d.id}><div><strong>{d.title}</strong><small>{d.originalName} · {d.expiresAt?`vence ${new Date(d.expiresAt).toLocaleDateString('pt-BR')}`:'sem validade'}</small></div><span className={`status ${d.status==='APPROVED'?'success':''}`}>{labels[d.status]}</span>{(d.correctionNotes||d.rejectionReason)&&<p>{d.correctionNotes||d.rejectionReason}</p>}</article>):<p>Nenhum documento enviado.</p>}</div></div></div></>}
/* eslint-disable @typescript-eslint/no-explicit-any, react-hooks/exhaustive-deps */

'use client';import Link from'next/link';import{useEffect,useState}from'react';import{api}from'@/components/platform-api';
export default function MyCourses(){const[d,setD]=useState<any>();useEffect(()=>{api('/learning/dashboard').then(setD)},[]);return <><div className="portal-page-head"><div><p className="eyebrow">APRENDIZAGEM</p><h1>Meus cursos</h1><p>Continue de onde parou e acompanhe o avanço da clínica.</p></div></div>{!d?<p>Carregando cursos…</p>:!d.hasAccess?<div className="portal-panel"><h2>Ative sua assinatura</h2><p>Uma assinatura ativa libera todas as trilhas.</p><Link className="btn primary" href="/checkout">Ver plano</Link></div>:<div className="course-grid">{d.courses.map((c:any,i:number)=>{const ids=c.modules.flatMap((m:any)=>m.lessons.map((l:any)=>l.id));const done=d.progress.filter((p:any)=>ids.includes(p.lessonId)&&p.completedAt).length;const pct=ids.length?Math.round(done/ids.length*100):0;return <article className="course-card" key={c.id}><div className={`course-cover cover-${i%3+1}`}><span>{pct}% concluído</span><b>▶</b></div><div><h2>{c.title}</h2><p>{c.shortDescription}</p><div className="progress"><i style={{width:`${pct}%`}}/></div><Link className="btn" href={`/area-cliente/cursos/${c.slug}`}>Continuar curso</Link></div></article>})}</div>}</>}
/* eslint-disable @typescript-eslint/no-explicit-any, react-hooks/exhaustive-deps */

'use client';import Link from'next/link';import{useEffect,useState}from'react';import{api,session}from'@/components/platform-api';
type Quote={enrollmentFeeInCents:number;monthlyFeeInCents:number;durationMonths:number;totalContractValueInCents:number};
export default function Checkout(){const[q,setQ]=useState<Quote>();const[accepted,setAccepted]=useState(false);const[msg,setMsg]=useState('');const[signed,setSigned]=useState(false);useEffect(()=>{api<Quote>('/subscriptions/quote').then(setQ).catch(e=>setMsg(e.message))},[]);async function finish(){setMsg('');try{await api('/subscriptions/checkout',{method:'POST',body:JSON.stringify({acceptContract:accepted})});setSigned(true)}catch(e){setMsg(e instanceof Error?e.message:'Erro no checkout')}}const money=(n=0)=>(n/100).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});if(signed)return <section className="section"><div className="wrap checkout-success"><p className="eyebrow">ASSINATURA ATIVA</p><h1>Bem-vindo à plataforma Florescência.</h1><p>Seu contrato demonstrativo foi registrado e o acesso aos cursos está liberado.</p><Link className="btn primary" href="/area-cliente/cursos">Acessar meus cursos</Link></div></section>;const logged=typeof window!=='undefined'&&session();return <section className="section"><div className="wrap checkout-layout"><div><p className="eyebrow">ASSINATURA PARA CLÍNICAS</p><h1>Prepare a equipe para uma gestão contínua.</h1><div className="portal-panel"><h2>1. Conta da clínica</h2>{logged?<p className="status success">Conta autenticada</p>:<><p>É obrigatório criar uma conta ou entrar antes da contratação.</p><div className="actions"><Link className="btn primary" href="/cadastro?next=/checkout">Criar conta</Link><Link className="btn" href="/entrar?next=/checkout">Já tenho conta</Link></div></>}</div><div className="portal-panel contract-box"><h2>2. Contrato de 24 meses</h2><p>A assinatura inclui todos os cursos publicados, acompanhamento de progresso e o ambiente de documentos LRSS.</p><label><input type="checkbox" checked={accepted} onChange={e=>setAccepted(e.target.checked)}/> Li e aceito o contrato demonstrativo, a taxa de inscrição e as 24 mensalidades.</label></div>{msg&&<p className="form-error">{msg}</p>}<button className="btn primary" disabled={!logged||!accepted} onClick={finish}>Confirmar assinatura mock</button></div><aside className="portal-panel order-summary"><p className="eyebrow">RESUMO</p><h2>Plano Florescência</h2><dl><div><dt>Inscrição</dt><dd>{money(q?.enrollmentFeeInCents)}</dd></div><div><dt>Mensalidade</dt><dd>{money(q?.monthlyFeeInCents)}</dd></div><div><dt>Prazo</dt><dd>{q?.durationMonths??24} meses</dd></div><div className="total"><dt>Valor contratual</dt><dd>{money(q?.totalContractValueInCents)}</dd></div></dl><small>Gateway abstrato em modo MOCK. Nenhuma cobrança real será feita.</small></aside></div></section>}

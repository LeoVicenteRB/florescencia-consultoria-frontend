import type{Metadata}from'next';import{Lora,Source_Sans_3}from'next/font/google';import{SiteFrame}from'@/components/site-frame';import'./globals.css';import'./responsive.css';import'./portal.css';import'./admin.css';const serif=Lora({subsets:['latin'],variable:'--font-serif',display:'swap'});const sans=Source_Sans_3({subsets:['latin'],variable:'--font-sans',display:'swap'});export const metadata:Metadata={title:{default:'Florescência | Gestão documental para odontologia',template:'%s | Florescência'},description:'Kit digital editável para apoiar a organização documental de clínicas odontológicas.',robots:{index:true,follow:true},openGraph:{locale:'pt_BR',type:'website'}};export default function Layout({children}:{children:React.ReactNode}){return <html lang="pt-BR" className={`${serif.variable} ${sans.variable}`}><body><a className="skip" href="#conteudo">Pular para o conteúdo</a><SiteFrame>{children}</SiteFrame></body></html>}
